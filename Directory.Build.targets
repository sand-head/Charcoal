<Project>
  <!-- The targets the package ships, for projects in this repo that reference the library directly. -->
  <Import Project="$(MSBuildThisFileDirectory)src/Charcoal/build/Charcoal.targets" Condition="'$(MSBuildProjectName)' != 'Charcoal'" />
</Project>
